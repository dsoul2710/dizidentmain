version: '3.8'

services:
  # PostgreSQL Database
  postgres:
    image: postgres:16-alpine
    container_name: clientxyz-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: clinic_hms_xyz
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: ${DB_PASSWORD:-xyz_secure_password_123}
    ports:
      - "5433:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
      - ./init-db.sql:/docker-entrypoint-initdb.d/init.sql:ro
    networks:
      - app-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Backend Spring Boot Application
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: clientxyz-backend
    restart: unless-stopped
    environment:
      SPRING_APPLICATION_NAME: clinic-hms-xyz
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/clinic_hms_xyz
      SPRING_DATASOURCE_USERNAME: postgres
      SPRING_DATASOURCE_PASSWORD: ${DB_PASSWORD:-xyz_secure_password_123}
      SERVER_PORT: 8080
      JWT_SECRET: ${JWT_SECRET:-xyz_change_this_secret_key_12345678901234567890}
      FILE_UPLOAD_BASE_DIR: /app/uploads
    ports:
      - "8081:8080"
    volumes:
      - ./uploads:/app/uploads
    networks:
      - app-network
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/actuator/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s

  # Frontend React Application
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        VITE_API_BASE: ${VITE_API_BASE:-http://localhost:8081}
        VITE_CLIENT_ID: clientxyz
        VITE_CLIENT_NAME: "XYZ Dental Clinic"
    container_name: clientxyz-frontend
    restart: unless-stopped
    ports:
      - "3001:80"
    networks:
      - app-network
    depends_on:
      - backend
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost/health"]
      interval: 30s
      timeout: 10s
      retries: 3

volumes:
  postgres-data:
    driver: local

networks:
  app-network:
    driver: bridge
