-- Initialize database for dev environment
-- This script runs automatically when the database is first created

-- Add any initialization SQL here
-- Tables will be created automatically by Spring Boot with ddl-auto=update

-- Example: Insert default admin user for dev environment
-- INSERT INTO users (mobile, password, role, is_active, created_at, updated_at)
-- VALUES ('9999999999', 'admin123', 'ADMIN', true, NOW(), NOW())
-- ON CONFLICT DO NOTHING;
